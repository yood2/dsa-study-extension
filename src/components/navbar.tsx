import { Button } from '@/components/ui/button';

function Navbar() {
    return (
        <>
            <Button>Home</Button>
            <Button>Browse</Button>
        </>
    );
}

export default Navbar;
